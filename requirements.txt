# --- Framework API ---
fastapi==0.139.0
# starlette pineado explícito: es dep transitiva de fastapi, pero se fija
# la versión parcheada (CVE-2025-54121, CVE-2025-62727, CVE-2026-48817/8, etc.)
# para que el security gate del CI (pip-audit) no la marque.
starlette==1.3.1
uvicorn[standard]==0.34.0

# --- Validación / settings ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Base de datos ---
sqlalchemy==2.0.36
# Driver Postgres (para docker-compose, Fase 2)
psycopg2-binary==2.9.10

# --- Cliente HTTP (para llamar a PokeAPI - Clase 4) ---
httpx==0.28.1

# --- Testing (Fase 1) ---
pytest==9.1.1 # >=9.0.3 corrige CVE-2025-71176
pytest-cov==7.1.0

# --- Monitoreo New Relic (Fase 6) ---
newrelic==10.4.0
