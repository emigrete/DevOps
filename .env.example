# =========================================================
#  PLANTILLA DE VARIABLES DE ENTORNO
#  Copiar este archivo a `.env` y completar con valores reales.
#  El archivo `.env` NUNCA se sube a git (está en .gitignore).
# =========================================================

# --- Aplicación ---
APP_NAME=tp-devops-api
APP_ENV=development
APP_PORT=8000

# --- Base de datos ---
# Para desarrollo local con SQLite:
DATABASE_URL=sqlite:///./app.db
# Para docker-compose con Postgres (Fase 2):
# DATABASE_URL=postgresql://devops:devops@db:5432/tpdevops
POSTGRES_USER=devops
POSTGRES_PASSWORD=devops
POSTGRES_DB=tpdevops

# --- API externa (Pokemon) usada para las trazas en monitoreo (Clase 4) ---
POKEAPI_BASE_URL=https://pokeapi.co/api/v2

# --- New Relic (Fase 6 - Monitoreo) ---
# Config 100% por env vars (sin newrelic.ini): el agente se arranca con
# `newrelic-admin run-program` (ver Dockerfile) y lee estas variables.
NEW_RELIC_LICENSE_KEY=your_license_key_here
NEW_RELIC_APP_NAME=tp-devops-api
# Distributed tracing → trazas encadenadas entre capas (on por defecto; explícito acá).
NEW_RELIC_DISTRIBUTED_TRACING_ENABLED=true
